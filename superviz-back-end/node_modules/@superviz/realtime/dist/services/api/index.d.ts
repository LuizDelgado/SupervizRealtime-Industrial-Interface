import { ComponentLimits } from './types';
export declare class ApiService {
    static createUrl(baseUrl: string, path: string, query?: {}): string;
    static validateApiKey(baseUrl: string, apiKey: string): Promise<any>;
    static fetchLimits(baseUrl: string, apikey: string): Promise<ComponentLimits>;
    static fetchApiKey(): Promise<string>;
    static sendActivity(userId: string): Promise<any>;
}
